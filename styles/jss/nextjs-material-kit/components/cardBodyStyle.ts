import createStyles from "@material-ui/styles/createStyles";
const cardBodyStyle = (theme: any) =>
  createStyles({
    cardBody: {
      padding: "0.9375rem 1.875rem",
      flex: "1 1 auto",
    },
  });

export default cardBodyStyle;
