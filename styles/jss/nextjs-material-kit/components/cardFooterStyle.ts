import createStyles from "@material-ui/styles/createStyles";
const cardFooterStyle = (theme: any) =>
  createStyles({
    cardFooter: {
      display: "flex",
      alignItems: "center",
      backgroundColor: "transparent",
      padding: "0.9375rem 1.875rem",
    },
  });

export default cardFooterStyle;
